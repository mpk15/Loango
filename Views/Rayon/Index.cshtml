@model IEnumerable<Loango_app.Models.Rayon>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.code_rayon)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.code_rayon)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_rayon }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_rayon }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_rayon })
        </td>
    </tr>
}

</table>
